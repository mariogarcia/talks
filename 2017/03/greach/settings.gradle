include 'slides'
include 'gql'
include 'domain'
include 'relayr'
include 'relays'
include 'bonddb'
include 'gorm'

rootProject.name = 'graphql-greach'

rootProject.children.each { project ->
    project.buildFileName = "${project.name}.gradle"
    assert project.buildFile.isFile()
}
