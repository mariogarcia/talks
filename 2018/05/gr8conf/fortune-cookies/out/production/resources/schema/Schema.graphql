type Queries {
  randomCookie: Cookie
  cookies(max: Int!, offset: Int!): [Cookie]
  login(credentials: Credentials!): UserProfile
}

type Mutations {
  createCookie(input: CookieInput): Cookie
}

schema {
  query: Queries
}
