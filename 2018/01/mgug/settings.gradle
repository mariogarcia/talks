include 'slides'
include 'bond-data'
include 'bond-gql-ratpack'
include 'bond-gorm-grails'

rootProject.name = 'graphql-mgug'

rootProject.children.each { project ->
    project.buildFileName = "${project.name}.gradle"
    assert project.buildFile.isFile()
}
